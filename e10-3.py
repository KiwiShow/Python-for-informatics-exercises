import stringfname = raw_input('Enter file name: ')try :	fhand = open(fname)except:	print 'This file cannot be opened: ',fname	exit()#  second step: get the dict,just like a histogramd = dict()	for line in fhand:	line = line.translate(None,string.punctuation)	line = line.lower()	words = line.split()	for word in words:		for single in word:			if single >= 'a' and single <= 'z':				d[single] = d.get(single,0) + 1# print dlst = list()total = 0for key,value in d.items():	lst.append((key,value))	total = total + valueprint 'total: ',total	lst.sort()print 'the alpha number : ',len(lst)	for key,value in lst:	print key,value,value/float(total)