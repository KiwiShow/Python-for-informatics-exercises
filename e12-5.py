import socketmysock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)mysock.connect(('www.py4inf.com', 80))mysock.send('GET http://www.py4inf.com/code/romeo.txt HTTP/1.0\n\n')doc = ''while True:    data = mysock.recv(512)    if ( len(data) < 1 ) :        break    doc += dataprint docpos = doc.find("\r\n\r\n")   #\r\n means starting another line. Double means there's a blank line between two linesprint doc[pos+4:]