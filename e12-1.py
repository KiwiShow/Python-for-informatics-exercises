import socketurl = raw_input('Enter: ')try:	words = url.split('/')# 	print words# 	['http:', '', 'www.py4inf.com', 'code', 'romeo.txt']	host = words[2]	mysock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)	mysock.connect((host, 80))	mysock.send('GET '+url+' HTTP/1.0\n\n')	except:		print 'The address is wrong or the URL not exists!'	exit()while True:    data = mysock.recv(512)    if ( len(data) < 1 ) :        break    print data,mysock.close()