try:	hour = raw_input("Enter hours:")	hour_float = float(hour)   #enter one and translate one	rate = raw_input("Enter rate:")	rate_float = float(rate)	except:	print 'Error,please enter numeric input'	quit() #if there is no quit() it will be error,because no hour_float	def computepay(hour_can,rate_can):			if hour_can > 40:		pay = 400 + (hour_can - 40) * rate_can * 1.5	else:		pay = hour_can * rate_can	print 'pay:',paycomputepay(hour_float,rate_float)