fname = raw_input('Enter file name: ')try :	fhand = open(fname)except:	print 'This file cannot be opened: ',fname	exit()#  second step: get the dict,just like a histogramd = dict()	for line in fhand:	words = line.split()	if len(words) >= 6  and words[0] == 'From':  #guardian pattern		hour = words[5].split(':')		d[hour[0]] = d.get(hour[0],0) + 1# print dlst = list()for key,value in d.items():	lst.append((key,value))	lst.sort()	for key,value in lst:	print key,value