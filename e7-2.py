fname = raw_input('Enter the file name: ')try:	fhand = open(fname)except:	print 'This file cannot be opened: ', fname	exit()	count = 0total = 0for line in fhand:	if line.startswith('X-DSPAM-Confidence:'):		count = count + 1		start = line.find(':')		sub_string = line[start+1:]		num = float(sub_string)		total = total + num	print 'Average spam confidence: ', total / count# e6-5 + 7-test03  combine them